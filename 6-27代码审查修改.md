# 6-27 代码审查修改

## 6. 2026-06-28：接收机 LoRa RX DMA 半传输重复回调修复

在不修改公共 `BSP_USB_UART_Driver.cpp` 的前提下，仅修改
`RC10_LIB/Module/Src/Module_lora.cpp`：

- `Lora_communication::Init()` 启动 ReceiveToIdle DMA 后关闭
  `DMA_IT_HT`，避免正常运行时产生半传输回调。
- `Lora_communication::RxCallback()` 使用
  `HAL_UARTEx_GetRxEventType()` 识别事件类型；若为
  `HAL_UART_RXEVENT_HT`，直接返回，不将数据写入通信 RX FIFO。
- IDLE 和 TC 事件继续按 HAL 给出的累计长度写入 FIFO。

该过滤用于防止 HT 先写入前半段、随后 IDLE/TC 又从缓冲区起点按累计
长度写入，从而造成前半段数据重复。由于 BSP 在回调之后重新启动 DMA，
HAL 可能再次开启 HT，因此回调中的事件类型过滤是最终防线。

遥控器 Comm 的 `Comm_StartRxDma()` 已在每次 DMA 启动成功后关闭
`DMA_IT_HT`，本轮无需修改。公共 BSP 文件仍保持未修改状态。

验证状态：HAL 驱动中已确认存在 `HAL_UARTEx_GetRxEventType()` 和
`HAL_UART_RXEVENT_HT`；已完成静态检查，仍需 Keil 编译及上机观察
`rx_crc_error_cnt`、`rx_drop_cnt` 和 `link_lost`。

> 实际修改日期：2026-06-28  
> 涉及工程：遥控器 STM32F4、接收机 STM32H7

## 1. TX `tx_busy` 超时恢复

### 问题

两端 Comm 在启动 UART TX DMA 时将 `tx_busy` 置 1，正常情况下依赖 LoRa AUX 上升沿再次进入发送函数解锁。如果 AUX 上升沿、EXTI 或任务通知丢失，`tx_busy` 可能永久保持 1，后续发送停止。

### 修改

遥控器和接收机统一增加：

```c
#define COMM_TX_BUSY_TIMEOUT_MS 100U
```

通信上下文增加：

```c
volatile uint32_t tx_busy_start_tick;
volatile uint16_t tx_timeout_recovery_cnt;
```

- 每次 TX DMA 开始时记录 `HAL_GetTick()`。
- Comm 周期任务每次先检查 `tx_busy`持续时间。
- 超过 100 ms 时调用 `HAL_UART_AbortTransmit()`，清除 `tx_busy` 和时间戳，`tx_timeout_recovery_cnt` 加 1。
- 超时恢复**不增加** `tx_error_cnt`。`tx_error_cnt` 仍只记录 HAL TX DMA 启动立即失败等原有错误。
- 不重新放回已经移入 DMA 缓冲区的数据，避免无线侧已收到时产生重复帧。
- 若 TX FIFO 仍有数据且 AUX 已为高电平，立即重新触发后续发送；AUX 为低时等待后续轮询或上升沿。

### 计数器导出

- 遥控器增加 `Communication_GetTxTimeoutRecoveryCnt()` 和全局调试快照 `g_comm_tx_timeout_recovery_cnt`。
- 接收机 `Communication` 增加 `GetTxTimeoutRecoveryCnt()`。
- 接收机 LoRa 的 `RC10_AirJoy_Data_S` 增加 `tx_timeout_recovery_cnt`，`Task_Process()` 周期刷新，`update_airjoy_data()` 向 FSM_Controller 使用的 `airjoy_data_` 输出。
- FSM_Controller 本身无需改代码，现有数据更新调用已能取得新字段。

## 2. RX DMA 半传输重复数据

### 检查结论

- 遥控器 Comm 的 `Comm_StartRxDma()` 在每次 ReceiveToIdle DMA 启动成功后已关闭 `DMA_IT_HT`，无需修改。
- 接收机 BSP 原本没有关闭 HT。DMA 达到半缓冲时可以先回调一次，后续 IDLE/TC 又按累计 `Size` 从缓冲区开头复制，存在前半段重复进入 RX FIFO 的风险。

### 处理决定

根据审查意见，已撤销 `BSP_USB_UART_Driver.cpp` 的全部修改，避免影响其他复用该 BSP 的 UART 任务。

因 ReceiveToIdle DMA 的启动和每次回调后重启都在 BSP 全局回调中完成，仅修改 Comm/LoRa 无法保证每次重启后关闭 HT。因此：

- 接收机 RX DMA 半传输重复数据风险当前保留。
- 本轮不通过 Comm/LoRa 层强行规避，以免将重复数据问题变为丢失后半包问题。
- 遥控器 Comm 现有的关闭 HT 逻辑保持不变。

## 3. 接收机 page 初始值

接收机 `Communication` 构造函数中：

```cpp
rec_page = 2;
```

首个有效遥控帧到达前，FSM_Controller 读到的默认 page 为 2；后续收到有效 JoystickFrame 后仍由帧内 `page` 正常覆盖。

## 4. 修改文件

### 遥控器

- `App/communication.h`
- `App/communication.c`

### 接收机

- `RC10_LIB/Module/Inc/Module_communication.h`
- `RC10_LIB/Module/Src/Module_communication.cpp`
- `RC10_LIB/Module/Inc/Module_lora.h`
- `RC10_LIB/Module/Src/Module_lora.cpp`

`FSM_Controller.cpp` 经检查无需修改。

## 5. 验证状态

### 已完成

- 静态确认两端均存在 100 ms 宏和独立超时计数器。
- 静态确认超时分支未增加 `tx_error_cnt`。
- 静态确认 `BSP_USB_UART_Driver.cpp` 本轮修改已全部撤销。
- `git diff --check` 通过。

### 待硬件/编译验证

- 遥控器和接收机 Keil 全量编译。
- 屏蔽 AUX 上升沿，确认约 100 ms 后 `tx_busy` 解锁、`tx_timeout_recovery_cnt` 加 1，`tx_error_cnt` 不变。
- 接收机 RX DMA 半传输重复数据仍需单独评估和验证，当前未修复。
