# 6-26 代码审查修改说明

本文档记录本次针对遥控器工程与接收机工程 `communication/lora` 模块的代码审查修改。修改目标是提高通信可靠性、DMA 状态恢复能力和故障可观测性，同时保持原有遥控器功能、按键映射、摇杆映射和无线协议帧格式不变。

## 一、不改变原有功能的保证

本次没有修改以下既有功能语义：

- 遥控器发送帧格式不变：`0xAA 0x55` 摇杆帧、`0xAA 0x66` 设置帧、`0xAA 0x77` 命令帧的字段顺序、帧尾和 CRC 覆盖范围均未改变。
- 接收机回传帧格式不变：`0x55 0xAA` XYZ 状态帧字段顺序、帧尾 `0xED` 和 CRC 覆盖范围未改变。
- 遥控器按键、拨杆、十字键映射不变。
- `update_airjoy_data()` 中右摇杆原有轴交换逻辑已保留：

```cpp
data->right_y = airjoy_data.right_x;
data->right_x = airjoy_data.right_y;
```

- `key_down_count` 仍然是累计上升沿计数；本次只修正 `key_pressed_count` 为“当前帧按下数量”，防止它跨帧累加。
- LoRa 模块 AUX 时序不变：AUX 高电平表示空闲，低电平表示忙。

换句话说，正常通信情况下，遥控器的使用手感、按键含义、摇杆方向、协议兼容性应保持原样。本次改动主要作用在异常情况：DMA 返回错误、FIFO 满、链路超时、H7 DCache 同步等。

## 二、接收机工程修改

涉及文件：

- `接收机h7代码/R1/RC10_LIB-FrameWork-main-v3/RC10_LIB-FrameWork-main/RC10_LIB/Module/Inc/Module_communication.h`
- `接收机h7代码/R1/RC10_LIB-FrameWork-main-v3/RC10_LIB-FrameWork-main/RC10_LIB/Module/Src/Module_communication.cpp`
- `接收机h7代码/R1/RC10_LIB-FrameWork-main-v3/RC10_LIB-FrameWork-main/RC10_LIB/Module/Inc/Module_lora.h`
- `接收机h7代码/R1/RC10_LIB-FrameWork-main-v3/RC10_LIB-FrameWork-main/RC10_LIB/Module/Src/Module_lora.cpp`

### 1. FIFO 增加丢包计数

原问题：

接收机侧 `comm_FIFO_t` 原本只有 `head/tail/buffer`，FIFO 满时直接丢弃新数据，但没有计数。现场调试时只能看到“没数据/卡住”，很难判断是链路没收到，还是 FIFO 溢出了。

修改：

```cpp
typedef struct {
    uint8_t* buffer;
    volatile uint16_t head;
    volatile uint16_t tail;
    volatile uint16_t drop_cnt;
} comm_FIFO_t;
```

并在 `FIFO_Push()` 中记录丢包：

```cpp
if (next != fifo.head) {
    fifo.buffer[fifo.tail] = data;
    fifo.tail = next;
} else {
    fifo.drop_cnt++;
}
```

示例：

如果接收机某段时间解析任务被阻塞，RX FIFO 被 DMA 回调不断写满，以前只会静默丢数据；现在可以通过 `GetRxDropCnt()` 判断是否发生过 FIFO 溢出。

对原功能影响：

正常 FIFO 未满时行为完全不变；只有 FIFO 满时多记录一个计数。

### 2. 命令帧计数真正生效

原问题：

`Module_communication.h` 中已有：

```cpp
uint8_t recv_command_cnts[9];
```

并且 `GetRecvCommandTotalCnt()` 会累加该数组。但旧代码解析 `0xAA 0x77` 命令帧后，只保存 `command/load1/load2`，没有递增数组，因此 `GetRecvCommandTotalCnt()` 没有实际意义。

修改：

在命令帧 CRC 和帧尾校验通过后加入：

```cpp
if (pCmdFrame->command < 9U) {
    recv_command_cnts[pCmdFrame->command]++;
}
```

示例：

遥控器发送三次 `command = 2`，接收机解析成功后：

```cpp
GetRecvCommandCnt(2)      // 返回 3
GetRecvCommandTotalCnt()  // 至少增加 3
```

对原功能影响：

原先已经保存的 `rec_command_command/load1/load2` 不变；只是补上原本预留但未工作的计数逻辑。

### 3. 初始化关键接收/发送状态

原问题：

部分成员变量依赖默认内存状态，尤其是：

- `rec_joystick[4]`
- `rec_send_key`
- `rec_page`
- `recv_command_cnts[9]`
- FIFO `drop_cnt`
- `tx_error_cnt`
- `joystick_frame_count`

上电后如果在第一帧合法数据到来前被读取，可能读到非预期值。

修改示例：

```cpp
rec_joystick[0] = 2048;
rec_joystick[1] = 2048;
rec_joystick[2] = 2048;
rec_joystick[3] = 2048;
rec_send_key = 0;
rec_page = 0;

for (uint8_t i = 0; i < 9; ++i) {
    recv_command_cnts[i] = 0;
}
```

示例：

接收机刚上电、遥控器还没开机时，摇杆默认中位 `2048`，按键默认未按下，而不是随机值。

对原功能影响：

合法帧到达后会立即覆盖这些默认值；正常通信时不会改变摇杆或按键解析结果。

### 4. TX/RX FIFO 临界区保护

原问题：

接收机 TX FIFO 可能被定时器发送、AUX EXTI 继续发送、普通接口发送同时访问。RX FIFO 也可能被 DMA 回调写入、主循环解析读取同时访问。`head/tail` 是 16 位变量，若并发更新，可能出现队列错位。

修改：

增加保存/恢复 PRIMASK 的临界区：

```cpp
static inline uint32_t EnterCritical()
{
    uint32_t primask = __get_PRIMASK();
    __disable_irq();
    return primask;
}

static inline void ExitCritical(uint32_t primask)
{
    if (primask == 0U) {
        __enable_irq();
    }
}
```

示例：

TX FIFO 搬运到 DMA 缓冲区时：

```cpp
uint32_t primask = EnterCritical();
count = FIFO_Count(tx_fifo);
for (uint16_t i = 0; i < count; i++) {
    FIFO_Pop(tx_fifo, dma_tx_buf[i]);
}
tx_busy = 1;
ExitCritical(primask);
```

对原功能影响：

只是保证原本的 FIFO 读写动作不可被中断打断，不改变发送内容和发送顺序。

### 5. H7 DCache 清理范围对齐

原问题：

STM32H7 开启 DCache 时，DMA 读取 TX buffer 前需要 clean cache。旧代码直接：

```cpp
SCB_CleanDCache_by_Addr((uint32_t*)dma_tx_buf, count);
```

这对 32 字节 cache line 对齐要求不够稳妥。

修改：

封装 32 字节对齐范围：

```cpp
uintptr_t start = reinterpret_cast<uintptr_t>(addr) & ~static_cast<uintptr_t>(31U);
uintptr_t end = (reinterpret_cast<uintptr_t>(addr) + len + 31U) & ~static_cast<uintptr_t>(31U);
SCB_CleanDCache_by_Addr(reinterpret_cast<uint32_t*>(start), static_cast<int32_t>(end - start));
```

示例：

如果本次 DMA 发送 17 字节，clean 实际覆盖所在的完整 32 字节 cache line，避免 DMA 读到旧数据。

对原功能影响：

发送数据内容不变，只提高 H7 DMA 与 DCache 一致性。

### 6. DMA 发送返回值检查

原问题：

旧代码调用 `HAL_UART_Transmit_DMA()` 后默认成功。如果 HAL 返回 `HAL_BUSY` 或 `HAL_ERROR`，`tx_busy` 可能一直保持忙，后续发送被卡住。

修改：

将底层发送接口改为返回 `HAL_StatusTypeDef`：

```cpp
virtual HAL_StatusTypeDef Comm_TxUseTxDMA(UART_HandleTypeDef * huart, uint8_t* data, uint16_t size)=0;
```

LoRa 实现：

```cpp
HAL_StatusTypeDef Lora_communication::Comm_TxUseTxDMA(UART_HandleTypeDef* huart, uint8_t* data, uint16_t size) {
    if (huart != nullptr && huart == lora_tx_huart) {
        return HAL_UART_Transmit_DMA(huart, data, size);
    }
    return HAL_ERROR;
}
```

失败时恢复状态：

```cpp
if (Comm_TxUseTxDMA(txhuart, dma_tx_buf, count) == HAL_OK) {
    tx_cnt++;
} else {
    uint32_t fail_primask = EnterCritical();
    tx_busy = 0;
    tx_error_cnt++;
    ExitCritical(fail_primask);
}
```

对原功能影响：

发送成功路径不变；只在 HAL 明确返回失败时解除卡死状态并计数。

### 7. 链路超时保护

原问题：

如果遥控器断电、LoRa 遮挡或链路中断，接收机可能继续保留最后一次合法摇杆/按键数据。对机器人控制来说，这属于高风险行为。

修改：

增加 200ms 超时保护：

```cpp
constexpr uint32_t LORA_LINK_TIMEOUT_MS = 200U;
```

通过合法摇杆帧计数判断链路是否还活着：

```cpp
uint32_t joystick_frame_count = GetRecvJoystickFrameCount();
if (joystick_frame_count != last_joystick_frame_count) {
    last_joystick_frame_count = joystick_frame_count;
    last_joystick_rx_tick = HAL_GetTick();
    link_lost = false;
} else if ((HAL_GetTick() - last_joystick_rx_tick) > LORA_LINK_TIMEOUT_MS) {
    link_lost = true;
    airjoy_data.left_x = 0.0f;
    airjoy_data.left_y = 0.0f;
    airjoy_data.right_x = 0.0f;
    airjoy_data.right_y = 0.0f;
    airjoy_data.key = 0;
    airjoy_data.key_pressed_count = 0;
}
```

并向外暴露：

```cpp
uint8_t link_lost;
bool is_link_lost() const { return link_lost; }
```

示例：

- 正常：遥控器每隔数毫秒持续发送摇杆帧，`link_lost = 0`。
- 异常：超过 200ms 没有新的合法摇杆帧，接收机将摇杆归零、按键清零，`link_lost = 1`。
- 恢复：重新收到合法摇杆帧后，`link_lost` 自动回到 0。

对原功能影响：

正常链路下不改变控制数据；只在链路中断时进入安全状态。

## 三、遥控器工程修改

涉及文件：

- `遥控器32f4主控代码/remote_control_twolora_puls/App/communication.h`
- `遥控器32f4主控代码/remote_control_twolora_puls/App/communication.c`

### 1. 临界区保存/恢复 PRIMASK

原问题：

旧代码多处直接：

```c
__disable_irq();
...
__enable_irq();
```

如果这些函数从中断上下文进入，直接 `__enable_irq()` 可能改变进入函数前的中断状态。

修改：

```c
static uint32_t Comm_EnterCritical(void)
{
    uint32_t primask = __get_PRIMASK();
    __disable_irq();
    return primask;
}

static void Comm_ExitCritical(uint32_t primask)
{
    if (primask == 0U) {
        __enable_irq();
    }
}
```

示例：

压入摇杆帧时：

```c
uint32_t primask = Comm_EnterCritical();
for (int i = 0; i < sizeof(JoystickFrame_t); i++) {
    FIFO_Push(&g_Comm.tx_fifo, ptr[i]);
}
Comm_ExitCritical(primask);
```

对原功能影响：

FIFO 写入内容和顺序不变；只是更安全地恢复中断状态。

### 2. DMA 发送移出关中断区域

原问题：

旧 `TxBufferToDMA()` 在关中断期间调用 `HAL_UART_Transmit_DMA()`。HAL 内部可能访问状态机、DMA 寄存器和回调相关状态，不适合放在全局关中断区域里。

修改：

关中断阶段只做 FIFO 搬运和 `tx_busy` 设置：

```c
uint32_t primask = Comm_EnterCritical();
uint16_t count = FIFO_Count(&g_Comm.tx_fifo);
...
g_Comm.tx_busy = 1;
Comm_ExitCritical(primask);
```

然后在临界区外启动 DMA：

```c
if (count > 0) {
    if (HAL_UART_Transmit_DMA(g_Comm.txhuart, g_Comm.dma_tx_buf, count) == HAL_OK) {
        tx_cnt++;
    } else {
        uint32_t fail_primask = Comm_EnterCritical();
        g_Comm.tx_busy = 0;
        g_Comm.tx_error_cnt++;
        Comm_ExitCritical(fail_primask);
    }
}
```

对原功能影响：

发送的数据、分包大小、AUX 触发机制不变；只改变 HAL 调用所在的中断状态。

### 3. RX DMA 启动统一检查返回值

原问题：

旧代码多处直接调用：

```c
HAL_UARTEx_ReceiveToIdle_DMA(...);
__HAL_DMA_DISABLE_IT(...);
```

如果 HAL 返回错误或 DMA handle 为空，代码仍继续执行。

修改：

封装为：

```c
static HAL_StatusTypeDef Comm_StartRxDma(void)
{
    HAL_StatusTypeDef status = HAL_UARTEx_ReceiveToIdle_DMA(g_Comm.rxhuart, g_Comm.dma_rx_buf, DMA_BUF_SIZE);
    if (status == HAL_OK && g_Comm.rxhuart->hdmarx != NULL) {
        __HAL_DMA_DISABLE_IT(g_Comm.rxhuart->hdmarx, DMA_IT_HT);
    } else if (status != HAL_OK) {
        g_Comm.rx_error_cnt++;
    }
    return status;
}
```

使用位置：

- `Communication_Task_Init()`
- `Comm_UartRx_Callback_Wrapper()`
- `Comm_UartError_Callback_Wrapper()`

对原功能影响：

成功路径与旧逻辑一致；失败时增加错误计数，方便定位接收 DMA 启动失败。

### 4. 增加 TX/RX 错误计数

新增字段：

```c
volatile uint16_t tx_error_cnt;
volatile uint16_t rx_error_cnt;
```

示例：

- `tx_error_cnt` 增加：说明 `HAL_UART_Transmit_DMA()` 返回了非 `HAL_OK`。
- `rx_error_cnt` 增加：说明 `HAL_UARTEx_ReceiveToIdle_DMA()` 启动失败。

对原功能影响：

只是增加观测变量，不参与正常业务分支。

## 四、特别说明：未修改的遥控器功能点

本次未改动：

- 摇杆 ADC 读取与校准逻辑。
- 遥控器按键扫描逻辑。
- `tx_button_state` 的位定义。
- HMI 串口屏命令含义。
- KFS 设置命令值。
- 三位拨杆、二位拨杆、肩键、十字键映射。
- LoRa AUX 高/低电平语义。
- 发送周期和定时器节奏。

这意味着遥控器正常使用时，用户层感受到的操作方式不应发生变化。

## 五、建议验证用例

### 1. 正常遥控器操作

操作：

1. 打开遥控器和接收机。
2. 推动四个摇杆。
3. 按下所有按键、拨动所有拨杆。

期望：

- 接收机收到的摇杆方向与修改前一致。
- 右摇杆轴交换仍按旧逻辑生效。
- 按键、拨杆、十字键含义与修改前一致。
- `link_lost = 0`。

### 2. 命令帧计数

操作：

遥控器串口屏发送 `command = 2` 三次。

期望：

```cpp
GetRecvCommandCnt(2) == 3
GetRecvCommandTotalCnt() >= 3
```

### 3. 链路断开保护

操作：

1. 正常连接后推摇杆，让接收机有非零控制量。
2. 关闭遥控器或遮挡 LoRa。
3. 等待超过 200ms。

期望：

- `link_lost = 1`。
- 摇杆归零。
- 按键清零。
- 链路恢复后 `link_lost = 0`。

### 4. DMA 发送失败恢复

操作：

人为制造 `HAL_UART_Transmit_DMA()` 返回 `HAL_BUSY` 或 `HAL_ERROR` 的场景。

期望：

- `tx_busy` 不会永久保持 1。
- `tx_error_cnt` 增加。
- 后续 AUX 空闲后仍可继续发送。

### 5. FIFO 溢出观测

操作：

人为阻塞接收机解析任务，持续输入数据。

期望：

- FIFO 满时 `drop_cnt` 增加。
- 系统不死锁。

## 六、已做的静态检查

已执行：

```powershell
git diff --check
```

结果：

- 没有补丁格式错误。
- 仅提示 Git 配置会在下次触碰文件时把 LF 转为 CRLF。

未执行 Keil/ARM 编译，原因是当前环境 PATH 中未找到：

- `UV4.exe`
- `armclang.exe`
- `armcc.exe`

因此最终仍建议在 Keil 中重新编译两个工程，并按第五节做硬件联调确认。
