# RC10 四舵轮工程 AI 上下文交接（2026-05-20）

生成时间：2026-05-20 17:55（Asia/Shanghai）  
仓库路径：`D:\desktop\2026RC\Control\2026RC-Team1-R1Code\RC10_LIB-FrameWork`  
当前分支：`Jia6_temp`  
当前 HEAD：`4bedf19`  
工作区状态：文档更新前为干净状态（`git status --short` 为空）

## 1. 当前目标

- 维持 RC10 四舵轮底盘控制链路稳定，持续推进“控制口径一致 + 调试链路可观测”。
- 与舵轮调试面板工程保持同一语义口径，便于串口联调与问题回放。
- 本轮重点是“上下文交接文档同步”，不引入新的控制逻辑改动。

## 2. 已完成事项（最近有效状态）

- 已完成 `setSpeed* / get*` 主接口外部坐标兼容：
  - 对外口径：`+x=车体右`，`+y=车体前`。
  - 内部口径仍保持：`+x=车体前`。
- 已修复 `setZeroCurrent()` 与上电回零冲突：
  - 回零未完成时自动忽略并清除零电流请求。
  - 避免“误打断回零后不恢复”的卡死风险。
- 已完成四舵轮主链路近期重构保存点，最近关键提交：
  - `4bedf19`：外部坐标兼容 + 零电流/回零互斥修复。
  - `51e43b9`：底盘遥测 V2 修复与文档目录迁移同步。

## 3. 进行中事项

- 底盘主视图方向一致性问题仍在联调中（RC10 与调试面板间的口径核对还在持续）。
- mode5 串口遥测字段收敛与显示策略仍在迭代，尚未形成最终稳定字段集。
- 注释编码历史问题已做修复，但仍需避免批量转码导致的二次污染。

## 4. 高风险认知点（接手必看）

- 坐标口径有“外部接口口径”与“内部控制口径”两层：
  - 外部主接口口径：`x右/y前`；
  - 内部运动学口径：`x前/y左`（或项目既定内部定义）。
- 遥测/面板出现“看起来方向反了”，先查“输入映射层”与“显示映射层”，不要先动控制律。
- `setZeroCurrent` 仅用于明确零电流意图，不能作为回零流程切换器。

## 5. 代码改动边界（本轮交接视角）

- 本轮文档不触碰电机接口层，不改接收机协议，不改 mode20/mode30 语义。
- 仅记录并承接当前底盘链路状态、符号口径、回零互斥语义与后续联调重点。

## 6. 验证状态与关键命令

- 已完成（状态核对）：
  - `git branch --show-current`
  - `git rev-parse --short HEAD`
  - `git status --short`
- 建议接手后第一轮验证命令：
  - `git log --oneline -n 8`
  - `rg -n "setZeroCurrent|setSpeed_LockNowYaw|getBodySpeed|applyDebugTargetOverride" User/Setup`

## 7. 可复现场景（方向/口径）

1. 左摇杆上推：检查 `vel_x` 正负与车体前进方向是否一致。  
2. 左摇杆左推：检查 `vel_y` 正负与车体左移方向是否一致。  
3. 右摇杆左推：检查 `right_x`、`omega_z` 与车体旋向是否一致。  
4. 回零期间触发 `setZeroCurrent`：确认回零流程不被打断。

## 8. 下一步优先级建议

1. 先做“输入映射层”与“遥测显示层”一一对照表，固定符号口径。  
2. 再做“主视图方向错位”问题闭环（先观测后改控制）。  
3. 最后再考虑遥测字段进一步裁剪，避免联调期过早删除诊断量。

## 9. 回退点与保存点

- 推荐回退锚点：`4bedf19`（当前 RC10 主状态锚点）。
- 若需对比前一状态：`d28fc1d`、`72acda5`（注释与控制改动修复链）。

## 10. 一句话交接结论

> RC10 当前重点已从“结构重构”转入“符号口径与联调一致性闭环”；坐标兼容与回零互斥已落地，下一位接手应优先完成方向显示与遥测口径的一致性验证。
